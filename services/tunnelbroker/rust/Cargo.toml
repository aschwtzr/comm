[package]
name = "rust-notifications"
description = "Library to send iOS and Android push notifications"
homepage = "https://comm.app"
version = "0.1.0"
license = "MIT"
edition = "2021"

[dependencies]

[lib]
crate-type=["staticlib"]
